import React,{ useState } from 'react'
import styled from "styled-components"
import { FcGlobe } from "react-icons"

export default function StartRoom(props) {
    const [room,setRoom] = useState("open")

  return (
    <div>
      <div></div>
      <div>+ Add a Topic</div>
    </div>
  )
}
